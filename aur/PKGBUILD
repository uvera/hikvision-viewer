# Maintainer: Your Name <you@example.com>
#
# GitHub releases: run scripts/release-github-aur.sh from the upstream repo to set url=,
# source= (tag archive), and sha256sums, and regenerate .SRCINFO. The tag tarball excludes
# aur/ (.gitattributes export-ignore) so checksum commits can match the archive. Copy PKGBUILD
# + .SRCINFO into your AUR clone and push to aur.archlinux.org.
#
# Local tarball (not GitHub): see scripts/package-release.sh — parent dir must be $pkgname-$pkgver.

pkgname=hikvision-viewer
pkgver=0.3.0
pkgrel=1
pkgdesc="PyQt6 multi-stream RTSP viewer for IP cameras"
arch=('any')
url='https://github.com/uvera/hikvision-viewer'
license=('MIT')
depends=('python' 'mpv' 'python-pyqt6' 'python-pyyaml' 'python-cryptography' 'python-keyring' 'python-dotenv' 'python-mpv')
makedepends=('python-build' 'python-installer' 'python-setuptools' 'python-wheel')
install=hikvision-viewer.install
source=("${pkgname}-${pkgver}.tar.gz::https://github.com/uvera/hikvision-viewer/archive/refs/tags/v0.3.0.tar.gz")
sha256sums=('SKIP')

build() {
  cd "${pkgname}-${pkgver}"
  python -m build --wheel --no-isolation
}

package() {
  cd "${pkgname}-${pkgver}"
  python -m installer --destdir="$pkgdir" --prefix=/usr dist/*.whl
  install -Dm644 config.example.yaml -t "${pkgdir}/usr/share/doc/${pkgname}/"
}
